---
title: "Stale Poetry and Audit Execution State Retired"
parent: changelog
status: published
tags: ["changelog", "maintenance", "reference"]
---

# Stale Poetry and Audit Execution State Retired

**Maintenance ID:** 0.1.00012.fnf-mech-012
**Date:** 2026-08-07
**Scope:** content/reference/fref-0900-poet.md; content/reference/audits/ (three records)

---

## What changed

- `content/reference/fref-0900-poet.md` — retained as a historical poetry-audit artifact only. Removed the Active Subagent Flight Manifest (twelve staged JULES-POET assignments), the `Staged` execution states, the target-file routing lists, the obsolete `.mdx` coordinate inventories, the generated Coverage Matrix, and the generated Density Extremes tables. The Archive Totals table was retained and explicitly labeled as a snapshot (values as of the original audit run, not current state). No replacement totals were invented; the in-record related verse was preserved verbatim.
- `content/reference/audits/fref-audt-case.md` — retained as a historical audit snapshot. Removed the 71-row generated path dump (legacy `.mdx` paths) and the current-sounding `Action Required` line; condensed the findings to a summary preserving the audit class (`CASENUM`), the 71-gap total, and the entry date. Status moved to `archived`.
- `content/reference/audits/fref-audt-cont.md` — retained as a historical audit snapshot. Removed the `Action Required` line; condensed the single containment finding to a summary preserving the audit class (`CONTAINMENT`), the 1-gap total, and the entry date. Status moved to `archived`.
- `content/reference/audits/fref-audt-intg.md` — retained as a historical audit snapshot. Removed the `Action Required` line; preserved the audit class (`INTEGRITY`), the 0-gap result, and the entry date. Status moved to `archived`.
- `content/changelog.md` — trunk count updated (5 → 6 records).
- `content/changelog/2026-08-07-fnf-mech-012.md` — this record.

## Historical evidence preserved

- The poetry audit's existence and its aggregate snapshot totals (563/3446 haiku, 562/5447 limerick, 563/4464 aphorism files/verses), explicitly labeled as snapshot values rather than present state.
- The occurrence, entry date (2026-07-13), audit class, and flagged-gap totals of all three audits.
- The related aphorism, haiku, and limerick verse attached to all four records, untouched.

## What was deliberately left alone

- No graph repair was attempted on the basis of the audits' old tables; the audit snapshots are historical, and the current repository checks govern.
- The work-order records `fref-0901` through `fref-0918` (which reference the retired flight manifest) were not touched; they are outside this pass's scope.
- No new doctrine, policy, or action queues were created.
- The reference trunk (`content/reference.md`, count 127) was not changed; no records were added or removed.

## Files touched

- `content/reference/fref-0900-poet.md`
- `content/reference/audits/fref-audt-case.md`
- `content/reference/audits/fref-audt-cont.md`
- `content/reference/audits/fref-audt-intg.md`
- `content/changelog.md`
- `content/changelog/2026-08-07-fnf-mech-012.md` — this record

## Verification performed

- `./bin/validate_graph.sh` passed (Filed graph, form IDs, HTML IDs, and publication checks).
- Residue scan: no flight-manifest rows, staged-state tables, coverage/density tables, `.mdx` path dumps, or `Action Required` lines remain in the four target files; the only remaining mentions of `JULES-POET` and `.mdx` are the explicit retirement notes describing what was removed. Confirmed in the compiled output: no "Active Subagent Flight Manifest" appears in any built page.
- Frontmatter keys in this record are within the established closed schema.

## Unresolved follow-up

- The retired flight manifest remains referenced by the work-order records (`fref-0901`–`fref-0918`) outside this pass's scope.
