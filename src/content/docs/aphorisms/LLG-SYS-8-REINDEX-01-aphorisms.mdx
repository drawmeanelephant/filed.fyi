---
title: "MA/8C Cluster Re-Indexing — Aphorisms"
description: "Acknowledgment that the document set has coalesced into a single interpretive cluster."
version: "Automated SOMA Sweep Cycle 2"
tags: ["reindexing", "cluster-identity", "bin-8c", "retroactive-identity"]
---

import Broside from '../../../components/Brosides.astro';

# Cluster Re-Indexing Aphorisms

<Broside type="tip" title="The cluster is hereby informed that it has always been a cluster." icon="layers" id="REINDEX-01-1">
The cluster is hereby informed that it has always been a cluster.
</Broside>

<Broside type="tip" title="Retroactive structural identity: the system declines to choose between versions and treats conflict as a stable feature." icon="refresh-cw" id="REINDEX-01-2">
Retroactive structural identity: the system declines to choose between versions and treats conflict as a stable feature.
</Broside>

<Broside type="tip" title="Any finer distinction between 'when' and 'since when' the cluster exists is left to the drift reports." icon="clock" id="REINDEX-01-3">
Any finer distinction between 'when' and 'since when' the cluster exists is left to the drift reports.
</Broside>
